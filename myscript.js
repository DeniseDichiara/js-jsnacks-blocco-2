let emptyList = []

let userNumber = parseInt(prompt('Enter a number'))

let sum = false

let i = 0

while( (userNumber < emptyList.length) && (sum === false) ){
    console.log(Empty[userNumber])

    if(Empty[userNumber])
}


//? Genara numeri random da 1 a 100
let randomNumber = Math.floor(Math.random() * (100 - 1) + 1);

let userNumberGuess = parseInt(prompt('Guess the number'))

let rightNumber = 0

while(userNumberGuess !== randomNumber){
    console.log (userNumber = false)
}