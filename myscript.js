let emptyList = []

let userNumber = parseInt(prompt('Enter a number'))

let sum = 50

let i = 0

while( (i < emptyList.length) && (sum === false) ){
    console.log(Empty[userNumber])

    if(Empty[userNumber])
}